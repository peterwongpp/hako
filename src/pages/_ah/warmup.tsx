import {Container, Row, Col} from 'react-bootstrap';

export default function Warmup() {
  // TODO: touch the firestore / cloud storage to make sure the instance is ready?
  return (
    <Container>
      <Row>
        <Col>
          <div>Success.</div>
        </Col>
      </Row>
    </Container>
  )
};
